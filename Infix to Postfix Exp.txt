#include<stdio.h>
#include<conio.h>
char pop();
void push();
int getprec(char);
char infix[20],stack[20],post[20],val;
int i=0,j=0,k=0;
void main()
{
clrscr();
char a;
int pre,pre1;
puts("enter the exressions");
gets(infix);
for(i=0;infix[i]!=NULL;i++)
{
val=infix[i];
switch(val)
{
case '(' : push();
break;
case ')' : while((a=pop())!='(')
post[j++]=a;
break;
case '+':case '-' :case '*' :case '^' :case '%'
pre=getprec(val);
pre!=getprec(stack[k-1]);
while(pre1>=pre)
{
a=pop();
post[j++]=a;
pre1=getprec(stack[k-1]);
}
push();
break;
default:post[j++]=val;
}
}
while((a=pop())!=NULL)
post[j++]=a;
puts(post);
getch();
}
int getprec(char item)
{
if(item=='+'|| item=='-')
return(1);
else if(item=='*'|| item=='/'||item'%')
return(2);
else if(item=='^')
return(3);
else
return(0);
}
char pop()
{
char a;
a=stack[k-1];
k--;
return a;
}
void push(0
{
stack[k]=val;
k++;
}


